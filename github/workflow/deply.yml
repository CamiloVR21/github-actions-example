name: Deply docker Container
# This workflow represents a set of basic End-to-End tests
on:
  push:
  #  branches:
  #    - '*'
  pull_request:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
     -  name: test run docker
      run: docker run hello-world

